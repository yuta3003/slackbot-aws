version: '3'

services:
  app:
    build:
      context: .
      dockerfile: ./docker/python3/Dockerfile
    volumes:
    - type: bind
      source: "./slackbot_aws"
      target: "/root/slackbot_aws/"
    tty: true
    env_file: .envrc
